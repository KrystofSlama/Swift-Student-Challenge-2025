import Foundation
import SwiftUI

struct ButtonLevel: View {
    
    var body: some View {
        RoundedRectangle(cornerRadius: 20)
            .foregroundStyle(Color(red: 238/255, green: 238/255, blue: 239/255))
            .frame(minWidth: 250)
    }
}
